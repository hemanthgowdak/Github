Git 
git config globally

From <http://guides.beanstalkapp.com/version-control/common-git-commands.html> 
$ git config user.email "my@emailaddress.com"
$ git config user.name "Brian Kerr"



Git Project Initialization

Git init
Git add .
Git commit -m "First Commit"

Create a new branch
git checkout -b html
git checkout -b css 
git checkout html
git checkout css
git checkout master 


--Yo
Git branch multiply
Git branch -a
Git checkout multiply
--After creating new File
git add index.css
--Staging File
Git commit -m "some changes"

--Check Difference in Master 
git diff master html

--Merger two Branches
(Merge on master branch)
Git merge html


--Git Hub 

Git Clone url
Git pull origin master
git add .
git commit -m "style changes to css"
git push origin master
